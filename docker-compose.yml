version: '3.8'
services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/go/src/github.com/dev-helper/dev-helper
    ports:
      - "40000:40000"
    environment:
      - GOFLAGS=-mod=mod
